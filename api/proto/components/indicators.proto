syntax = "proto3";

package components;

option go_package = "/components";

message Indicators {
  optional bool simulated = 1;
  optional bool exercise = 2;
  optional bool emergency = 3;
  optional bool c2 = 4;
  optional bool egressable = 5;
  optional bool starred = 6;
}
